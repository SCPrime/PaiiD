# PaiiD Backend Environment Variables Template
# Copy this to your Render service environment variables

# =============================================================================
# REQUIRED VARIABLES (Application will fail without these)
# =============================================================================

# API Authentication
API_TOKEN=your-secure-api-token-here

# Alpaca API - PAPER TRADING EXECUTION ONLY
ALPACA_PAPER_API_KEY=your-alpaca-paper-api-key
ALPACA_PAPER_SECRET_KEY=your-alpaca-paper-secret-key

# Tradier API - MARKET DATA, NEWS, QUOTES (Primary data source)
TRADIER_API_KEY=your-tradier-api-key
TRADIER_ACCOUNT_ID=your-tradier-account-id

# Sentry Error Tracking - REQUIRED for production monitoring
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_ENVIRONMENT=production

# =============================================================================
# OPTIONAL VARIABLES (Application will work with defaults)
# =============================================================================

# Anthropic API (AI fallback for market data)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Redis Cache
REDIS_URL=redis://user:password@host:port

# Logging Configuration
LOG_LEVEL=INFO

# CORS Configuration
ALLOW_ORIGIN=https://your-frontend-domain.com

# Trading Mode
LIVE_TRADING=false
TRADING_MODE=paper

# Test Configuration (for deterministic Playwright tests)
USE_TEST_FIXTURES=false

# =============================================================================
# RENDER-SPECIFIC VARIABLES (Auto-configured by Render)
# =============================================================================

# These are automatically set by Render, don't override:
# - PORT (automatically assigned)
# - RENDER_EXTERNAL_URL (your service URL)
# - RENDER_SERVICE_ID (unique service identifier)

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Go to your Render dashboard
# 2. Select your backend service
# 3. Go to Environment tab
# 4. Add each variable above with your actual values
# 5. Make sure to mark sensitive variables as "Secret"
# 6. Click "Save Changes"
# 7. Redeploy your service

# =============================================================================
# SECURITY NOTES
# =============================================================================

# - Never commit actual API keys to git
# - Use strong, unique API tokens
# - Rotate keys regularly
# - Monitor usage in your API provider dashboards
# - Enable 2FA on all API accounts

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# If deployment fails:
# 1. Check all REQUIRED variables are set
# 2. Verify API keys are valid and active
# 3. Check service logs for specific error messages
# 4. Run pre-launch validation: python scripts/prelaunch.py --strict