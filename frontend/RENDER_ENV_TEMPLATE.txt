# PaiiD Frontend - Render/Vercel Environment Variables
# Copy these into the hosting provider dashboard before deploying.

# Application profile + routing
NEXT_PUBLIC_APP_ENV=production
NEXT_PUBLIC_BACKEND_API_BASE_URL=https://paiid-backend.onrender.com

# Shared secrets (mirror backend values)
API_TOKEN=<same-as-backend-API_TOKEN>
NEXT_PUBLIC_API_TOKEN=<same-as-backend-API_TOKEN>

# Observability & release metadata
NEXT_PUBLIC_SENTRY_DSN=<sentry-project-dsn>
NEXT_PUBLIC_RELEASE_VERSION=<git-tag-or-semver>
# Hosting platforms automatically inject commit SHA (VERCEL_GIT_COMMIT_SHA).

# Feature toggles
NEXT_PUBLIC_TELEMETRY_ENABLED=true

# Optional AI integrations
NEXT_PUBLIC_ANTHROPIC_API_KEY=<optional-anthropic-key>

# Runtime defaults
NODE_ENV=production
PORT=3000
